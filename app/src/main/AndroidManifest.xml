<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/genius"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StudentMenu"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/genius">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <activity
            android:name=".AboutUs"
            android:label="@string/genius">
        </activity>

        <activity
            android:name=".student.quiz.QuizQuestion"
            android:configChanges="orientation|screenSize"
            android:label="@string/quiz">
        </activity>

        <activity
            android:name=".student.quiz.QuizResult"
            >
        </activity>

        <activity android:name=".Register"></activity>
        <activity android:name=".Forgot_Password" />
        <activity android:name=".Login1" />
        <activity android:name=".MainMain"></activity>
        <activity android:name=".TutorMenu"></activity>
        <activity android:name=".tutor.quiz.AddQuiz"></activity>
        <activity android:name=".tutor.quiz.AddQuizDetail"></activity>

        <activity android:name=".student.course.ComputerScience" />
        <activity android:name=".student.course.CourseContent" />
    </application>



</manifest>